// 부서 드롭다운 api
export interface DepartmentsDropdown {
  departmentId: string;
  departmentName: string;
}

// 직급 드롭다운 api
export interface PositionsDropdown {
  positionId: string;
  positionName: string;
}
